module.exports = {
    Query: {
        users: (root, args) => {
        // users: (root, args, { models, req }) => {
            // return models.User.find();
            return [];
        },
    }
}